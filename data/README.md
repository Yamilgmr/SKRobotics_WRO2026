# Data

This folder stores calibration data and test measurements.

Current status: pending real measured data from the physical robot.

Planned data files:

- `calibration/ultrasonic_distance_samples.csv`
- `calibration/servo_angle_limits.csv`
- `calibration/motor_pwm_response.csv`
- `calibration/open_challenge_turn_tuning.csv`
- `calibration/obstacle_color_samples.csv`

Each dataset should include date, robot version, code commit, battery voltage, and notes.

Test data must be measured on the real robot. Do not invent rows and present them as real measurements. If an example row is needed for formatting, mark it clearly as `example only` or keep the value as `TBD` until the test is performed.

